# Profile: Vishal LIVE (real money)
# Runs in parallel with vishal (paper) — completely isolated
# Own DB, own dashboard, own logs
# REAL MONEY — most conservative settings

profile:
  name: "vishal-live"
  display_name: "Vishal (Live)"

dhan:
  client_id: "1110941563"
  api_key: "UPDATE_WITH_NEW_KEY"
  api_secret: "UPDATE_WITH_NEW_SECRET"
  totp_secret: "UPDATE_WITH_NEW_TOTP"
  pin: "UPDATE_WITH_NEW_PIN"

database:
  path: database/vishal-live.db

dashboard:
  api_dir: dashboard/api/vishal-live

intraday:
  daily_capital_limit: 10000
  per_trade_max_capital: 4000
  max_trades_per_day: 2
  daily_loss_limit: 600
  min_confidence_score: 8
  vix_threshold: 16
  price_range_min: 100
  price_range_max: 2000

fno:
  mode: "paper"
  paper_capital: 50000
  daily_capital_limit: 50000
  per_trade_max_capital: 25000
  daily_loss_limit: 5000
  max_lots_per_trade: 1
  min_confidence_score: 8
  vix_threshold: 18
